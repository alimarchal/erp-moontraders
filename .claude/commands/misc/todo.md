Create Tax Rates views (index, create, edit, show, partials/form-fields)
Create Product Tax Mappings views (index, create, edit, show, partials/form-fields)
Create Tax Transactions views (index, create, edit, show, partials/form-fields)
Add routes for all three modules to web.php
Update settings index with cards for all three modules
Run migrate:fresh --seed to test everything